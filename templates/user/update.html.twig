{% extends 'base.html.twig' %}

{% block body %}
{% block main %}




    {{ form_start(userForm, {'attr': {'novalidate': 'novalidate'}} ) }}

    <div class="container" >
        <div class="row">
            {% if user.urlPhoto == null %}
                <div class="col"> <img src="{{ asset('photos/photoDefaut.png') }}" style="display: block; width: auto; height: auto; max-width: 14em; max-height: 14em; margin-bottom: 1em"></div>
          {% else %}
            <div class="col"> <img src="{{app.request.basepath}}/photos/{{ user.urlPhoto }}" style="display: block; width: auto; height: auto; max-width: 14em; max-height: 14em; margin-bottom: 1em"></div>
            {% endif %}
            <div class="col"><h1>{{ user.nom }}</h1></div>
        </div>

        <div class="row">
            <div class="col">{{ form_label(userForm.username) }}</div>
            <div class="col">{{ form_widget(userForm.username) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.prenom) }}</div>
            <div class="col">{{ form_widget(userForm.prenom) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.nom) }}</div>
            <div class="col">{{ form_widget(userForm.nom) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.telephone) }}</div>
            <div class="col">{{ form_widget(userForm.telephone) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.email) }}</div>
            <div class="col">{{ form_widget(userForm.email) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.site) }}</div>
            <div class="col">{{ form_widget(userForm.site) }}</div>
        </div>
        <div class="row">
            <div class="col">{{ form_label(userForm.fileTemp) }}</div>
            <div class="col">{{ form_widget(userForm.fileTemp) }}</div>
        </div>
        <hr>
        <div class="row">
            <div class="col">{{ form_label(userForm.password) }}</div>
            <div class="col">{{ form_widget(userForm.password) }}</div>
        </div>
        <i style="font-size: 14px; color: #AA3333">obligatoire pour tout changement</i>
        <hr>
        <div class="row">
            <div class="col">{{ form_widget(userForm.Enregistrer) }}</div>
            <div class="col"><a href="{{ path('affichage_sortie') }}"><input class="btn-annuler-modif-profil" type="button" value="Annuler"></a></div>
            <div class="col"><a href="{{ path('change_password', {'id': user.id}) }}"><input class=" btn-changer-mdp" value="Changer mot de passe" type="button"></a></div>
        </div>
    </div>
    {{ form_end(userForm) }}

{% endblock %}
{% endblock %}

{% block title %}Update user! | {{ parent() }}{% endblock %}